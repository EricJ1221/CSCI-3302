


//write a recursive helper method called findMax
//that takes an array of int called values as an 
//argument and returns the largest value found
//in the array

public int findMax(int[] array) 
{
   return this.findMax(int[] array, int index, int max)
}

   private int findMax(int[] array, int index, int max) 
   {
      if (index >= this.collection.length)
         return 
   }